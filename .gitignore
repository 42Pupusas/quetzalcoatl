/target

# Editor / tooling local config
/.claude/

# Profiling artifacts (perf record + flamegraph output)
perf*.data
perf*.data.old
flamegraph*.svg

# dhat heap profiles (diagnostic artifacts)
dhat-*.json
